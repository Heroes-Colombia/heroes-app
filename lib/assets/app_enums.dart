enum BusinessCategories {
  ropa,
  comida,
  belleza,
}

enum UserPermissions {
  admin,
  user,
  business,
  beneficiary,
}

enum UserStatus {
  active,
  pending,
}

enum BusinessStatus {
  active,
  inactive,
  pending,
}
